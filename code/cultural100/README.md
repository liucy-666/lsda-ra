# code/cultural100

受控 100 文化对的构造脚本。

- `build_cultural_pairs_100.py`：生成 `code/mmdit_causal/cultural_pairs_100.json` 的源脚本（100 对，含 short / long / SS / SL / LL 提示词）。
- 该 JSON 是 EXP_3 / EXP_6 的冻结输入；修改配对必须新建版本，不得覆盖。
